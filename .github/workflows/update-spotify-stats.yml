name: Update Spotify Statistics

on:
  # Ejecutar diariamente a las 9:00 AM UTC
  schedule:
    - cron: '0 9 * * *'
  
  # También ejecutar manualmente cuando se necesite
  workflow_dispatch:

# Dar permisos al workflow
permissions:
  contents: write
  pull-requests: write

jobs:
  update-stats:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        pip install requests beautifulsoup4
        
    - name: Update Spotify statistics
      run: |
        python3 spotify_web_scraper.py
        
    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        # Verificar si hay cambios
        if git diff --quiet data/spotify_stats.json; then
          echo "No hay cambios en las estadísticas"
        else
          git add data/spotify_stats.json
          git commit -m "🤖 Auto-update Spotify statistics: $(date '+%Y-%m-%d %H:%M')" || echo "No hay cambios para commitear"
          git push origin main || echo "Error en push, pero continuando..."
          echo "✅ Estadísticas actualizadas automáticamente"
        fi
        
    - name: Update HTML if stats changed
      run: |
        # Verificar si hay cambios en el JSON
        if ! git diff --quiet data/spotify_stats.json; then
          echo "📊 Actualizando HTML con nuevas estadísticas..."
          python3 update_stats.py
          git add index.html
          git commit -m "🤖 Auto-update HTML with latest Spotify stats"
          git push
        fi
